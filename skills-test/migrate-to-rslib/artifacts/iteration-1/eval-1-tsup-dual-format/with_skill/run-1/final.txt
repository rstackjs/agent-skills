Migrated the library from tsup to Rslib.

- Added rslib.config.ts, preserving ESM/CJS outputs, declarations, clean builds, and source maps.
- Updated `package.json` scripts and replaced `tsup` with `@rslib/core`; lockfile updated.
- Removed obsolete `tsup.config.ts`.
- Kept package export paths unchanged: `dist/index.js`, `dist/index.cjs`, and `dist/index.d.ts`.

Validated successfully:

- `npm run build`
- `npm run typecheck`
- CJS and ESM consumer scripts
- Presence of both bundles, declaration file, and both source maps
- Watch mode startup and incremental rebuild

Remaining note: watch mode emits `EMFILE` file-watcher-limit warnings in this sandbox, despite building and rebuilding successfully.