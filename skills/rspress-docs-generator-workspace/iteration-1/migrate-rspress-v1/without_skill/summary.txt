Rspress v1 to v2 Migration Summary
====================================

Fixture: migrate-rspress-v1
Approach: General knowledge (no skill used)

Changes Made:
-------------
1. package.json:
   - Updated @rspress/core from ^1.47.2 to ^2.0.14
   - Removed @types/react and @types/react-dom (no longer needed with React 19)
   - Updated react and react-dom from ^18.0.0 to ^19.0.0
   - Kept @types/node at ^20.0.0

2. rspress.config.ts:
   - No changes needed. The import `from '@rspress/core'` and config structure remain compatible in v2.

3. docs/index.md:
   - No changes needed. Markdown frontmatter and content are compatible.

4. tsconfig.json:
   - No changes needed.

Build Result:
-------------
Build PASSED successfully with Rspress v2.0.14.

Output files generated:
- doc_build/index.html
- doc_build/404.html
- doc_build/static/js/* (bundled JS)
- doc_build/static/css/* (bundled CSS)
- doc_build/static/search_index.en.*.json

Notable issues:
- None. The migration was straightforward because the site was minimal.
- One correction during migration: initially assumed the package was renamed to `rspress` for v2, but discovered that `rspress` latest is still v1 (1.47.1) while `@rspress/core` latest is v2 (2.0.14). The package name did not change.
