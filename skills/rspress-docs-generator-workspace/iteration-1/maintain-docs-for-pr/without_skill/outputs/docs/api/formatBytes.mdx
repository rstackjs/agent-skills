---
title: formatBytes
---

# formatBytes

Formats a byte count into a human-readable string.

## Signature

```ts
function formatBytes(bytes: number, opts?: { decimals?: number }): string;
```

## Parameters

| Name | Type | Description |
|------|------|-------------|
| `bytes` | `number` | The byte count to format. |
| `opts` | `object` | Optional configuration. |
| `opts.decimals` | `number` | Number of decimal places (default: `2`). |

## Returns

A formatted string such as `'1 KB'` or `'1.50 MB'`.

## Examples

```ts
import { formatBytes } from 'format-bytes-project';

formatBytes(0);                        // '0 B'
formatBytes(1024);                     // '1 KB'
formatBytes(1048576);                  // '1 MB'
formatBytes(1048576, { decimals: 1 });  // '1.0 MB'
formatBytes(1536, { decimals: 1 });     // '1.5 KB'
```
