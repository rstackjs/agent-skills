Migrated the library from tsup to Rslib.

- Added rslib.config.ts with ESM/CJS builds, declarations, clean output, and source maps.
- Updated build/watch scripts and dependencies in package.json.
- Updated `package-lock.json` and removed obsolete `tsup.config.ts`.
- Kept package exports and output filenames unchanged: `dist/index.js`, `dist/index.cjs`, and `dist/index.d.ts`.

Validated with:

- `npm run build`
- `npm run typecheck`
- ESM and CommonJS consumer tests
- Source-map source references
- `npm pack --dry-run`
- `git diff --check`

No functional follow-ups identified.