diff --git a/package.json b/package.json
index 8f7418a..da5b8e8 100644
--- a/package.json
+++ b/package.json
@@ -16,12 +16,12 @@
     "dist"
   ],
   "scripts": {
-    "build": "tsup",
-    "build:watch": "tsup --watch",
+    "build": "rslib",
+    "build:watch": "rslib --watch",
     "typecheck": "tsc --noEmit"
   },
   "devDependencies": {
-    "tsup": "^8.5.0",
+    "@rslib/core": "^0.19.1",
     "typescript": "^5.8.3"
   }
 }
diff --git a/tsup.config.ts b/tsup.config.ts
deleted file mode 100644
index ed3d319..0000000
--- a/tsup.config.ts
+++ /dev/null
@@ -1,11 +0,0 @@
-import { defineConfig } from 'tsup';
-
-export default defineConfig({
-  clean: true,
-  dts: true,
-  entry: {
-    index: './src/index.ts',
-  },
-  format: ['esm', 'cjs'],
-  sourcemap: true,
-});
